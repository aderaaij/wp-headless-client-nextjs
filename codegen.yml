schema: ${SCHEMA_URL}
documents:
  - './src/graphql/**/*.ts'
generates:
  ./src/types.d.ts:
    plugins:
      - typescript
pluckConfig:
  gqlMagicComment: GraphQL
  globalGqlIdentifierName:
    - gql
    - graphql
