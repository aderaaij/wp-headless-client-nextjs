schema: ${WORDPRESS_API_URL}
documents:
  - './src/graphql/**/*.ts'
generates:
  ./src/types.ts:
    plugins:
      - typescript
pluckConfig:
  gqlMagicComment: GraphQL
  globalGqlIdentifierName:
    - gql
    - graphql
